window.APP_CONFIG = window.APP_CONFIG || {
  // Leave empty to use same origin (recommended for full-stack deployment).
  // Set this to your backend URL when using GitHub Pages for frontend.
  apiBaseUrl: ''
};
